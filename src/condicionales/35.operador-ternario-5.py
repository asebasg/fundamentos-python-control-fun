dividendo = 10 # Se declara el dividendo con valor 10
divisor = 0 # Se declara el divisor con valor 0
resultado = dividendo / divisor if divisor != 0 else "División por cero no permitida" # Operador ternario para evitar división por cero
print(resultado) # Imprime el resultado o mensaje de error
