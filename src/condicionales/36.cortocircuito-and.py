lista = [] # Se crea una lista vacía

if lista and lista[0] == 'Python': # Cortocircuito: si lista está vacía, no evalúa lista[0]
    print("El primer elemento es 'Python'.") # Esta línea no se ejecuta porque lista está vacía
