numeros = [0, 0, 1, 0] # Se crea una lista con algunos ceros y un uno

if any(numeros): # La función any() devuelve True si al menos un elemento es verdadero (no cero)
    print("Al menos un número es no cero.") # Se ejecuta porque hay un 1 en la lista
