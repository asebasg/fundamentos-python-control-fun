nombres = ["Ana", "Carlos", "Elena"]  # Se crea una lista de nombres
for i in range(len(nombres)):  # Se itera sobre los índices de la lista usando range(len(nombres))
    print(f"Posición {i}: {nombres[i]}")  # Se imprime la posición y el nombre correspondiente
